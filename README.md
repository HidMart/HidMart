# HidMart

HidMart is an async Python framework for building token-based bots for Bale Messenger.

## Installation

Install directly from GitHub:

```bash
pip install https://github.com/YOUR_USERNAME/HidMart/archive/main.zip --force-reinstall
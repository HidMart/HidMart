[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hidmart"
version = "0.1.0"
description = "Async Python framework for Bale token bots"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

authors = [
    {name = "HidMart Team"}
]

dependencies = [
    "httpx>=0.27,<1.0"
]

[project.urls]
Homepage = "https://github.com/YOUR_USERNAME/HidMart"
Repository = "https://github.com/YOUR_USERNAME/HidMart"

[tool.setuptools.packages.find]
include = ["hidmart*"]